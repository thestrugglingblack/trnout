'use strict';
const getEvents = require('../api/get-events');
const getEvent = require('../api/get-event');
const getAttendees = require('../api/get-attendees');

module.exports = {
  getEvents,
  getEvent,
  getAttendees
};
